mutation resourceLike($input: ResourceLikeInput!) {
	resourceLike(input: $input) {
		resource {
			id

			slug
			title
			subtitle
			body
			resourceImageURL
			topicName
			authorId
			numberOfLikes
			isLikedByCurrentUser
			publishDate
		}
	}
}
