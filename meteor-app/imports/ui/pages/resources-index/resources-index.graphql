query resourcesIndexPage($currentPageNum: Int!) {
	searchArticles(pageNum: $currentPageNum) {
		totalCount
		nodes {
			slug
			title
			articleImageURL
			topicName
			publishDate
		}
	}

	articleTopics {
		topicName
		iconImageURL
	}
}
