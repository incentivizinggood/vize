query resourcesIndexPage($currentPageNum: Int!) {
	searchArticles(pageNum: $currentPageNum) {
		totalCount
		nodes {
			id
			slug
			title
			publishDate
		}
	}

	articleTopics {
		topicName
		iconImageURL
	}
}
