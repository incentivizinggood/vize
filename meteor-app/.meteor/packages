# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base@1.3.0             # Packages every Meteor app needs to have

# Build Plugins - These probably cannot be replaced until we drop Meteor because
#                 they are closely integrated with Meteor's build system.
blaze-html-templates@1.0.4    # Compile .html files into Meteor Blaze views
standard-minifier-css@1.4.0   # CSS minifier run for production mode
standard-minifier-js@2.3.1    # JS minifier run for production mode
es5-shim@4.7.0                # ECMAScript 5 compatibility for older browsers
ecmascript@0.10.6             # Enable ECMAScript2015+ syntax in app code
barbatus:typescript           # Compile TypeScript
swydo:graphql                 # Compile .graphql files
nathantreid:css-modules       # Allows us to have private CSS in a module

# Dev Tools - These help work with Meteor, but are not needed to make the app work.
shell-server@0.3.1            # Server-side component of the `meteor shell` command

# UI
gadicc:blaze-react-component  # Render Blaze templates in React components. Used for the Autoform forms.
react-template-helper         # Render React components in Blaze templates. Used for the Autoform forms.
aldeed:autoform               # A framework for making forms from SimpleSchema.
twbs:bootstrap                # Needed for the navbar dropdown to work.
dandv:jquery-rateit           # Makes the star rating inputs on the review form.

# I18n
universe:i18n                 # Internationalization framework. We use this to translate the UI in English and Spanish.
universe:i18n-blaze

# Users and Accounts - These handle authentication.
#                      These are the last things that depend on MongoDB.
#                      To be replaced by Passport.js after the Meteor methods are migrated to GraphQL mutations.
accounts-password@1.5.0

# Data and API - These integrate with Meteor and its data layer.
mongo@1.4.2                   # The database Meteor supports right now
apollo@3.0.1                  # GraphQL integration with Meteor. Needed for server to know what user made a query.
reactive-var@1.0.11           # Reactive data-type for tracker. Only used in Autoform forms. Everywhere else, use stateful React components.
reactive-dict                 # Same as reactive-var, but can hold several variables instead of just one.
tracker@1.1.3                 # Meteor's client-side reactive programming library
react-meteor-data             # Enables React components to work with Meteor's reactive data system. Will be removed when we migrate fully to GraphQL.
aldeed:schema-index           # Needed for the unique constraints in SimpleSchema.

# Simple helpers - Libraries that are not strongly coupled to anything. Should be easy to replace.
check                         # An assertion util for blocking method inputs with the wrong type.
okgrow:analytics              # Enables Google Analytics for the app.
email                         # Allows us to send emails with Mailgun.

# Unknown - These packages have not yet been accounted for.
mobile-experience@1.0.5       # Packages for a great mobile UX
mrgalaxy:stripe
